\ProvidesPackage{scuthesis}[2016/03/15 v0.1]

% 定义自己常用的宏包
\RequirePackage{multirow}
\RequirePackage{tabularx} % 固定宽度的表格
%\RequirePackage{diagbox} % 表格中的反斜线
%\RequirePackage{float} % 使用 H 参数强制图片在此排版，不建议
%\RequirePackage{rotating} % 图形、表格旋转排版

% 定义自己常用的东西
\newcommand\teacher{\scu@csupervisor@value}

\endinput
